import axios from "axios"


export const queryKey = ['repoData']

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search/team/sql1`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}

export const queryFn = async (q:any, oq: any) => {
    const { data } = await axios.get(`${process.env.NEXT_PUBLIC_API_URL}/search`,
   { params : {q:'player', oq:''}} );
    return data
}